// config first when install git in system

config --global user.name <your user name>

config --global user.email <your email> 

//after that you use thees comand in your woring directory(folder)

1) git status                           // to check git status.
 
2) git init                             // to create a git file if it not present i thr folder.

3) git add <file name>                  // to ad file in git 


Note :- you will also use (git add .) to add all files in one click.


4)git commit -m "massag section"        // to save add files in git and (-m) is massag.


After thees steps you connect your git to github repo file (whear thees git file are store) and parforn same other commands.

for connectin the files you get thees type of code in your repo file.

 sampal code( not actual ) :-
 
1) git remote origin https://github.com/<your user name on github>/<your repo name on github>.git
  
                  //after that you will check {origin} using (git remote -v) command

2) git branch -M master                 //to set branch in github

3) git push -u origin master            // to insert your file into github repo folder

     That is the all how you make a git file and repositori at git and github 
                                                  
                            Thanks and good bye:)                              


 
